node_modules
.DS_Store
*.log
.env
.env.local
bun.lockb
.parcel-cache

# dist/ is intentionally NOT ignored — dist/launcher.cjs is the Node-side
# launcher that ships with the package and is needed by the install script.
